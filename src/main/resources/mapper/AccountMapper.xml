<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xcy.mapper.AccountMapper">
    <select id="selectUserMsg" parameterType="account">
        select * from account where phoneName = #{phoneName}
    </select>

    <update id="updateMsgById" parameterType="account">
        update account
        set
        id = #{id},
        headImg = #{headImg},
        nickName = #{nickName},
        sex = #{sex},
        birthday = #{birthday};
        phoneNum = #{phoneNum};
        where id = #{id}
    </update>
    <update id="saveMsgById" parameterType="account">
        update account
        set
        id = #{id},
        headImg = #{headImg},
        nickName = #{nickName},
        sex = #{sex},
        birthday = #{birthday};
        phoneNum = #{phoneNum};
        where id = #{id}
    </update>

    <select id="selectUserLevel" parameterType="account">
        select accountLevel from account where phoneName = #{phoneName}
    </select>

    <update id="updateAccountMoney" parameterType="account">
        update account
        set
        money = money + #{money}
        where id = #{id}
    </update>
</mapper>