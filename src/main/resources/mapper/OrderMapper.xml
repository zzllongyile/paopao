<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xcy.mapper.OrderMapper">
    <select id="selectAllOrder" parameterType="order">
        select * from order where phoneName = #{phoneName} and status = #{status}
    </select>

    <select id="selectUnpaidOrder" parameterType="order">
        select * from order where phoneName = #{phoneName} and status = #{status}
    </select>

    <select id="selectWaitOrder" parameterType="order">
        select * from order where phoneName = #{phoneName} and status = #{status}
    </select>

    <select id="selectUnderwayOrder" parameterType="order">
        select * from order where phoneName = #{phoneName} and status = #{status}
    </select>

    <select id="selectPlaceOrder" parameterType="order">
        select * from order where phoneName = #{phoneName} and status = #{status}
    </select>

</mapper>